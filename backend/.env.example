PORT=3000
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=troque-por-um-segredo-forte

# PostgreSQL (Prisma — usuários, tenants, IAM)
DATABASE_URL=postgresql://usuario:senha@localhost:5432/assetra?schema=public

# MongoDB (Mongoose — inventário de ativos e histórico)
MONGODB_URL=mongodb://localhost:27017/assetra

NODE_ENV=development
